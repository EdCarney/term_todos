#include "logger.hpp"
#include <string>
#include <iostream>
#include <chrono>

namespace logger {

    logger::logger(char *log_file, bool verbose) {
    }

    logger::~logger() {
    }

    void logger::log(std::string msg, log_level level) {
    }
}
